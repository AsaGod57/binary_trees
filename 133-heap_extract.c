#include "binary_trees.h"

/**
 * heap_to_sorted_array - Starts to convert a Binary Max Heap
 * @root: Specifies double pointer to the root node
 * Return: The value stored in the root node (Ends)
 */
int heap_extract(heap_t **root)
{
	return (root);
}
