#include "binary_trees.h"

/**
 * heap_to_sorted_array - Starts to convert a Binary Max Heap
 * @heap: Specifies pointer to the root node of the heap to convert
 * @size: Specifies address to store the size of the array
 * Return: The array sorted (Ends)
 */
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{
	return (heap);
}
